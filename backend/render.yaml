services:
  - type: web
    name: business-closure-prediction-rag
    env: python
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: GEMINI_API_KEY
        sync: false
